import { createContext } from 'react';

// Create a context with a default value of null and Used as named export
export const UserContext = createContext(null);

// Export the context as default
//export default UserContext;
